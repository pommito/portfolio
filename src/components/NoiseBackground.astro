<svg class="absolute pointer-events-none cursor-none">
  <filter id="grainy">
    <feTurbulence type="turbulence" baseFrequency="0.3"></feTurbulence>
    <feColorMatrix type="saturate" values="0"></feColorMatrix>
  </filter>
</svg>